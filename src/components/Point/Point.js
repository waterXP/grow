import React, { Component } from 'react';
// import PropTypes from 'prop-types';

import './Point.css';

class Point extends Component {
  render () {
    // let props = this.props;
    return (
      <div className='point bordered'>
      </div>
    );
  }
}

export default Point;


        // <ProgressBar />
        // <button onClick={props.confirm.bind(this, 'main')}>完成</button>
        // <button onClick={props.cancel.bind(this)}>放弃</button>